# This should be a python file i guess?
# Needs to spit out: 
  # Data for fig 1c
  # 2100 confidence intervals for version in which we cluster by country, and 
  # do a standard version of a bootstrap
